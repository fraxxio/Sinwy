BETTER_AUTH_SECRET="" 				# Must match the backend's BETTER_AUTH_SECRET
BETTER_AUTH_URL="" 				# Provide a value for BETTER_AUTH_URL

# Backend origin used for server-side fetches and the dev proxy.
BACKEND_URL=http://localhost:3001
